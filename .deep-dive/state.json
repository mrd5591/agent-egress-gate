{
  "lastRunCommit": "817ce899a50cca7532645aac43475cca16bbc3ae",
  "lastRunTimestamp": "2026-09-08T15:05:00Z",
  "mode": "full",
  "filesAnalyzed": 52,
  "findingsCount": 61,
  "note": "First deep-dive on this repo; establishing pass. Baseline was clean: 6/6 packages green, 93.2% coverage, golangci-lint 0 issues.",
  "coverageGaps": [
    "The race detector was NOT run: this machine has no C compiler, so 'go test -race' fails to build and scripts/coverage.sh cannot run as CI runs it. Every concurrency conclusion this pass is source-reading, not observed behaviour. CI does run it.",
    "terraform fmt/validate, tflint and govulncheck were NOT executed (not installed). All Terraform findings are from reading HCL.",
    "Terraform has never been applied (no AWS account), and 'validate' does not inspect container_definitions at all. The container_definitions block was reviewed by hand only.",
    "Signal handling (SIGINT/SIGTERM) was read, not exercised under a real signal: the host is Windows.",
    "The docker container job WAS run locally and passed, including the new record-count assertions."
  ]
}
